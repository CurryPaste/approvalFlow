
import Vue from 'vue'

import FormControls from '@/components/FormControls/index.js'
Vue.use( FormControls )


import {
    Drawer, 
    Input, 
    InputNumber, 
    Select, 
    Option, 
    Radio, 
    RadioGroup, 
    Button, 
    Tabs, 
    TabPane, 
    Row, 
    Col, 
    Switch, 
    Checkbox, 
    Dialog, 
    CheckboxGroup,
    Popover,
    Tooltip
} from 'element-ui'
Vue.use( Drawer );
Vue.use( Input );
Vue.use( InputNumber );
Vue.use( Select );
Vue.use( Option );
Vue.use( Radio );
Vue.use( RadioGroup );
Vue.use( Button );
Vue.use( Tabs );
Vue.use( TabPane );
Vue.use( Row );
Vue.use( Col );
Vue.use( Switch );
Vue.use( Checkbox );
Vue.use( Dialog );
Vue.use( CheckboxGroup );
Vue.use( Popover );
Vue.use( Tooltip );
